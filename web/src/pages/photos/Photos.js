import React from "react";
import "./Photos.less"
import {connect} from "react-redux";

class Photos extends React.Component {
    render() {
        return (<div>9999</div>);
    }
}

export default Photos = connect((state) => ({...state}))(Photos);